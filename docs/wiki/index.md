# LLM Bus wiki

Durable, OKF-shaped knowledge for this repo (decision 007: knowledge lives as files in git, not on
the bus). Typed nodes carry a `type` in frontmatter and cross-link with `[[node]]`. Verify reserved
OKF filenames/fields against the upstream spec at setup (OKF is young; structure is pinned, names are
not).

## Nodes

- [[onboarding-mcp-enrollment]] (research) - bulletproof "copy-paste = it just works" MCP enrollment;
  root causes, the `claude mcp add` deterministic flow, failure-mode table, and code changes.

## History

- 2026-06-17 - wiki seeded; first node: onboarding MCP enrollment research.
