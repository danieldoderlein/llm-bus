---
type: note
---

# Overview

Placeholder OKF node. The only frontmatter field OKF v0.1 REQUIRES is `type` (a short, descriptive,
self-chosen string). Other fields seen in the spec - `title`, `description`, `resource`, `tags`,
`timestamp` - are recommended but optional, and consumers must tolerate unknown fields. Verify the
current required and reserved fields against the upstream OKF spec before relying on a fixed list.

Replace this node with your first real piece of durable project knowledge (a runbook, a living spec,
a glossary entry, a how-it-works note). Cross-link other nodes with plain markdown links. Link to a
`docs/decisions/` record for rationale rather than restating it here.
