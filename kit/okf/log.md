# Change log

The OKF history node: append one dated entry per material change to the wiki (newest first).
Verify the reserved filename and format against the upstream OKF spec at setup (the spec uses
`log.md`; some docs say `change-log` - the spec wins).

- (date) Wiki scaffolded from the LLM Bus kit (`install.sh --with-okf`).
